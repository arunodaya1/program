#include <stdio.h>
void swap(int * n1 ,int * n2);

int main() {
    int x=50;
    int y=60;
    printf("x is %d , y is %d\n",x,y);
    swap(&x,&y);
    printf("x is %d , y is %d\n",x,y);
    return 0;
}
void swap(int * n1,int * n2){
    int temp;
    temp=*n1;
    *n1=*n2;
    *n2=temp;
}
